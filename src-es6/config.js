'use strict';



/*const PACKAGE = require('../package.json');*/

//默认缓存key
const CACHE_DEFAULT_REQUEST_TYPE = 'request';

const config = {
    version: "4.0.0",
    cacheDefaultRequestType: CACHE_DEFAULT_REQUEST_TYPE,
    debug: true,
    monitor: true
};

export default config;

